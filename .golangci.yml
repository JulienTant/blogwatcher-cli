version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - bodyclose
    - errorlint
    - noctx
    - sqlclosecheck
    - testifylint

formatters:
  enable:
    - gofmt
    - goimports
